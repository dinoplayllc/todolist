<%- include("partials/header.ejs")%>


    <div class="container-fluid">
        <div class="row no-gutters">
            
            <% if(loggedIn === false){ %>

             <% }else { %>

                <% for (let i = 0; i<motherName.length; i++){%>
          <div class="d-flex col-12 col-lg-4 col-md-6 col-sm-12 marauto flex-column">
            <div class="cellMother jmborder motherTag mt-3">
                <input type="checkbox" id="myCheckbox<%= i %>" onchange="performAction(<%= i %>, '<%= encodeURIComponent(JSON.stringify(motherName[i])) %>')">
                <i class="fa-solid fa-trash"></i> |&nbsp;
                <span id="cellText<%= i %>">
                <%=motherName[i].name%>
                </span>
            </div>
            
            <div class="cell hide p-2" id="pdfID<%=i%>">
                <p>show pdf here</p>
            </div>
            <div class="cell p-2" id="staticNotes<%=i%>">
                <%=motherName[i].notes %>
            </div>
                <div class="cell p-2 hide" id="textareaMNotes<%=i%>">
                    <textarea name="updateMNotes<%=i%>" id="updateMNotes<%=i%>" cols="30" rows="10"><%=motherName[i].notes %></textarea>
                </div>

                <div class="d-flex flex-row-reverse flex-column">
                    <div class=" mx-2">
                        <button onclick="showPDF(<%=i%>);" class="btn btn-light">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-caret-right-fill" viewBox="0 0 16 16">
                                <path d="m12.14 8.753-5.482 4.796c-.646.566-1.658.106-1.658-.753V3.204a1 1 0 0 1 1.659-.753l5.48 4.796a1 1 0 0 1 0 1.506z"/>
                              </svg>
                        </button>
                    </div>
                    <div class=" ">
                        <button  class="btn btn-light" id="showHide<%= i %>" onclick="showHideFunc(<%=i%>);changeText(<%=i%>);">Update</button>
                    </div>
                    <div class=" ">
                        <button  class="btn btn-light hide" id="upload<%= i %>" onclick="">Upload</button>
                    </div>
                    <div class=" ">
                        <button class="btn btn-light hide" id="updateConfirm<%= i %>" onclick="updateFunc(<%=i%>,'<%= encodeURIComponent(JSON.stringify(motherName[i])) %>')">Confirm</button>
                    </div>
                </div>
          </div>
          <%}%>
            <%}%>
        </div>

    </div>


<%- include("partials/footer.ejs")%>
